# behat.yml

default:
    autoload:
        '': %paths.base%/features/bootstrap
    suites:
        default:
            paths:
                - 'features'
fullRun:
    suites:
        current:
            paths:
                - 'features'
        parent:
            paths:
                - 'vendor/erwanncordon/behatMain/BehatMain/src/features'

